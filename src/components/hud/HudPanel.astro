---
interface Props {
  as?: 'section' | 'article' | 'aside' | 'div';
  class?: string;
}

const { as: Tag = 'section', class: className = '' } = Astro.props;
---

<Tag class:list={['hud-panel', className]}>
  <slot />
</Tag>
